# Frontend Configuration
# VITE_REACTVIZ_API_URL=
VITE_ENABLE_LOCAL_ANALYSIS_FALLBACK=true

# Backend Configuration
NODE_ENV=production
PORT=4000

# Database Configuration (optional - uses default if not set)
# Defaults to ./data/reactviz.db in the project root
# DATA_DIR=/app/data
